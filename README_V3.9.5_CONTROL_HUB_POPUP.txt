V3.9.5 — MOBILE CONTROL HUB POPUP FIX

Fixes the issue shown in the mobile screenshot:
- Control Hub menu no longer renders inside/over the hero content.
- When opened, it becomes a viewport-level popup below the top header/control.
- Dashboard title and analytics remain visible behind/above the popup.
- Menu has its own scroll area.
- Explicit Close Operations button stays at the bottom.
- Corporate Operations mark is used on the trigger.
- Desktop is unchanged.
- Backend is unchanged.

Replace admin.html and dashboard.css. admin.js is included for version consistency.
Code.gs is included unchanged.
